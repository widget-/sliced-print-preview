#version 300 es
// shadow.frag — GLSL ES 3.0
// Minimal fragment shader for depth-only shadow map pass.
// No outputs needed — depth is written automatically.
precision highp float;

void main() {
    // Depth written automatically to the depth attachment
}
